"""Pure statistical computation (no Altair).

Reserved for the omnibus tests, post-hoc tests, effect sizes, and the
descriptive report builder that back the annotation constructors in
``layers.py`` (e.g. ``add_pvalue``).  Chart-annotation wrappers live in
``layers.py``; this module holds only the statistics.
"""
